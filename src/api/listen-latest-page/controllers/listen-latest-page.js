'use strict';

/**
 * listen-latest-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::listen-latest-page.listen-latest-page');
